# Evaluation

`run_evaluation.py` produces `results/baseline_report.json` from actual execution: RMSE (Spark evaluator), Precision@K, Recall@K, NDCG@K, group precision, group disparity, popularity exposure, genre diversity, and catalog coverage for both ALS and FairLens. Run with fixed configuration and preserve the output; do not hand-enter values.
